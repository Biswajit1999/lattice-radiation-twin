# Website interaction references

Accessed 2026-09-10. These sources guide a later website phase; they do not override the scientific gates or authorize fabricated values. Every quantitative display must read generated, provenance-linked repository output and retain the labels `OBSERVED`, `INFERRED`, `SIMULATED` or `FORECAST`.

- [Motion](https://motion.dev/) is the current code library for React, JavaScript and Vue animation. If adopted, the React site should use the current Motion package and documentation rather than obsolete Framer Motion imports. Suitable uses include filter-state transitions, restrained spring response and layout changes that help preserve object identity.
- [Framer's Motion dictionary](https://www.framer.com/dictionary/framer-motion/) documents the Framer Motion to Motion naming transition and distinguishes the code library from Framer's visual product. The repository requires React, TypeScript and Three.js, so the code library is the relevant reference.
- [Framer motion components](https://www.framer.com/community/marketplace/components/tags/motion/) and [21st.dev](https://21st.dev/) are interaction-pattern catalogs only. A component must not be copied or imported until its license, dependency weight, accessibility, reduced-motion behavior and maintenance status are checked.
- [UI UX Pro Max](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) supplied local searchable design guidance. Its generated cyberpunk/neon recommendation was rejected because it conflicts with the requested restrained scientific presentation. The retained guidance concerns confidence-band charts, text and table alternatives, line styles in addition to color, keyboard-operable 3D cameras, WebGL fallback and live reduced-motion handling.

The intended visual direction is deep navy and charcoal with restrained cyan and amber accents, generous negative space and explicit uncertainty. Avoid neon glow, glitch effects, scanlines, generic dashboard card grids and decorative motion. Animation should explain a state change or causal relationship, use transform and opacity where possible, remain interruptible, and expose a stable final frame when `prefers-reduced-motion` is active. A live media-query listener must respond when that preference changes.

Three-dimensional detector and environment views are scientific schematics. They require an equivalent 2D plot or data table, a concise text summary, an accessible canvas label, keyboard camera controls, pause controls for continuous motion, and a useful fallback when WebGL is unavailable. Particle effects may appear only in clearly labelled schematic or simulated views and must never imply observed counts or trajectories.
